# CCB-1 coverage audit

Generated by `python scripts/audit_ccb1_coverage.py` from the local raw cache and normalized files. This is a coverage audit, not a quality endorsement.

## Current materialization

| Source | Raw basis | Raw count | Normalized records | Coverage | Handling |
|---|---:|---:|---:|---:|---|
| Amazon ESCI | examples parquet rows | 2,621,288 | 10,000 | 0.3815% | explicit v0.1 normalization cap |
| Wayfair WANDS | label rows | 233,448 | 233,448 | 100% | all downloaded labels normalized |
| Google ConvApparel V1 | turn-recommendation rows | 175,751 | 175,751 | 100% | all V1 recommendation rows normalized |
| **Total** | — | — | **419,199** | — | — |

ConvApparel V2 is now archived and normalized separately for evaluation: `3,534` conversations / `141,168` turn-recommendation records. It is not mixed into the CCB-1 v0.1 split while the rights registry keeps its training status `REVIEW_REQUIRED`.

ESCI product rows are a separate product table (`1,814,924` rows) and are joined by source keys; they are not additional context records. ConvApparel contains `4,146` conversations and `14,736` turns. Its normalized rows preserve recommendation metadata but do not invent a universal relevance label.

## Split and provenance

The corrected CCB-1 split contains `348,974` train, `38,175` validation, and `33,190` hidden-test records. The previous integrity audit remains preserved at [`reports/ccb1_integrity_audit.md`](ccb1_integrity_audit.md) and reports zero source-group leakage and zero exact normalized-context collisions across splits.

## Findings

1. The repository does not currently exploit the full ESCI release; the 10,000-row cap is an explicit v0.1 storage/review boundary.
2. ConvApparel V2 is now present as a separately normalized evaluation-only addition. It is not silently merged into CCB-1 v0.1.
3. WANDS is fully represented at the label-row level, while `Partial` remains distinguishable from `Exact` and is not silently promoted.
4. The 419,199 count is real normalized coverage, not a claim that every source release is fully materialized.
5. No training set is promoted by this audit. Dataset rights and training status are recorded separately in [`datasets/registry.yaml`](../datasets/registry.yaml).

Machine-readable details: [`artifacts/ccb1_coverage_audit.json`](../artifacts/ccb1_coverage_audit.json).
