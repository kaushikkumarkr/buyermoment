# Service cost/performance profile

No Phase 9 Azure inference or deployment job was submitted. Therefore posted and estimated Azure dollars are unavailable and are not fabricated.

The tested default uses local Python/SQLite, lexical retrieval, and deterministic rules. The service path makes no paid model call in tests. Azure Search/Foundry/Content Understanding are provider seams for a later costed deployment after client volume and data complexity justify them.
