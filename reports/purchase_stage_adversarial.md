# Purchase-stage adversarial benchmark

The benchmark contains 300 controlled adversarial records across 30 categories.

- Accuracy: **0.5667**
- Macro F1: **0.6256**

The result is intentionally reported separately from the clean hidden benchmark. Errors are retained in `artifacts/phase4_failure_cases.jsonl`; no synthetic label is treated as a human outcome.
